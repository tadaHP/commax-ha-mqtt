spring:
  application:
    name: Wallpadcontroller
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update 
    open-in-view: false
  datasource:
    url: jdbc:sqlite:./mydb.db
    driver-class-name: org.sqlite.JDBC